import axios from "axios";

const axiosFetch = async (endpoint, method) => {
  if(method === 'get'){
    const fetch = await axios.get();

  }
  if(method === 'post'){
    
  }
  if(method === 'post'){
    
  }
  if(method === 'post'){
    
  }
  if(method === 'post'){
    
  }
  
  console.log(fetch);
};
axiosFetch("google.com", "get");
